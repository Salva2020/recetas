doctype html
html
	head
		meta(charset="utf-8")
		title Actualiza receta
	body
		h2 Selecciona la receta que quieres actualizar:
		if registros
			form(id="seleccionaRecetaForm" action="/seleccionaReceta" method="post")
				p
					select(name="nombresRecetas" form="seleccionaRecetaForm")
						each registro in registros
							option(value=registro.id_receta) #{registro.nombre}//# indica contenido de la plantilla pero en el anterior, reg.id_receta es js
				p
					input(type="submit" value="Muestra Receta")
		if registros2
			form (id="actualizarRecetaForm" action="/actualizaReceta/actualizar" method="post")
				input(type="hidden" name="id_receta" value=registro2[0].id_receta)
				p
					label(for="nombreReceta") Nombre:
					input(id="nombreReceta" type ="text" name="nombre" value=registros2[0].nombre)
				p
					label(for="descReceta") Receta:
					input(id="descReceta" type ="text" name="descripcion" value=registro2[0].descripcion)
				p
					input(type="submit" value="Actualizar")
					input(type="hidden" name="metodo" value="put")